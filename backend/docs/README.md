# Backend Documentation

Use this space for service-specific notes:

- API contracts and command schemas.
- Experiment orchestration flow charts.
- Deployment/runbook instructions.

Add subfolders as needed (e.g., `api/`, `commands/`, `experiments/`).
