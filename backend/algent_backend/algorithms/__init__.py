"""
Algorithms live here.

Each algorithm should expose a small interface (e.g., `run(input, config)`)
that can be orchestrated by experiments. Keep implementations self-contained
and dependency-light where possible.
"""


def list_available() -> list[str]:
    """Return registered algorithms. Replace with registry discovery later."""
    # TODO: implement registry/auto-discovery once algorithm set grows.
    return []

