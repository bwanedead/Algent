"""
Foundation layer for the agent system.

Houses the low-level building blocks an agent stands on — starting with the
model resolution layer (``models/``), which turns a neutral ``ModelSpec`` into a
concrete, runnable model object behind a swappable target seam.
"""
